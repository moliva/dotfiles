#!/usr/bin/env sh
npm install -g live-server # live reload for websites

npm install -g pug-cli # html dsl cli

npm install -g nodemon # live reload for node apps
npm install -g forever # runs node apps

npm install -g bower # module manager
npm install -g flightplan # deploy app to host through ssh with rsync

# build tools
npm install -g gulp
npm install -g grunt-cli

npm install -g surge # deploy to surge.sh

# project generators
npm install -g yo
npm install -g nwb

# yo generators
# npm install -g webapp-generator # not found
# npm install -g angular-generator 
npm install -g express-generator
# npm install -g generator-zurb-ink # generator for zurb email inliner

npm install -g js-beautify
npm install -g tldr

